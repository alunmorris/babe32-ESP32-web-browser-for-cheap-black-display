// Copy this file to secrets.h and fill in your values.
// secrets.h is listed in .gitignore and must not be committed.
#pragma once

// PHP proxy (primary fallback — handles Brightdata retry internally)
#define SECRET_PHP_HOST  "your-proxy-host.example.com"
#define SECRET_PHP_PATH  "/babe32proxy.php"
#define SECRET_PHP_TOKEN "your-proxy-token"
